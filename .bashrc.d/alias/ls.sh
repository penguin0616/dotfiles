#!/usr/bin/env bash
# I'm not sure where I got all these, but I think I grabbed them from default .bashrc's over time.
alias ll='ls -alF'
alias la='ls -A' # List out "Almost all" (i.e. everything except . and ..)
alias l='ls -CF'
alias lh="ls -lh" # h: File size get simplified
